apiVersion: route.openshift.io/v1
kind: Route
metadata: 
  labels: 
    app.kubernetes.io/instance: go-bezukmoc
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: go-bezukmoc 
  name: go-bezukmoc
spec:
  port:
    targetPort: 8081 
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: go-bezukmoc
    weight: 100 
